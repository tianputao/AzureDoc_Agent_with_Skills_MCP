# 渐进式披露架构

## 核心理念

**关键点：所有MCP工具在初始化时注册，但SKILL.md告诉Agent何时使用它们**

这种设计允许：
- 一个系统中有多个MCP Server
- 不同的Skill指导使用不同MCP Server的工具
- 完全通过SKILL.md内容控制工具使用逻辑
- 易于扩展新的Skills和MCP Servers

## 架构流程

### 1. 初始化阶段（一次性）

```
┌─────────────────────────────────────────────────────────────┐
│ 系统初始化                                                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ 1. 加载所有SKILL.md的metadata                               │
│    ├─ name: microsoft-docs                                 │
│    ├─ description: "Query official Microsoft docs..."      │
│    └─ (完整内容暂不加载，节省token)                           │
│                                                             │
│ 2. 连接所有MCP Server                                       │
│    ├─ Microsoft Learn MCP (http://localhost:3000)         │
│    ├─ GitHub MCP (假设未来添加)                            │
│    └─ Internal Docs MCP (假设未来添加)                     │
│                                                             │
│ 3. 注册所有MCP工具到Agent                                   │
│    all_mcp_tools = [                                       │
│        microsoft_docs_search,                              │
│        microsoft_docs_fetch,                               │
│        microsoft_code_sample_search,                       │
│        github_search_repos,     (假设)                     │
│        internal_search          (假设)                     │
│    ]                                                       │
│                                                             │
│    agent = chat_client.as_agent(                          │
│        instructions=system_instructions,                   │
│        tools=all_mcp_tools  ← 关键：所有工具都注册了       │
│    )                                                       │
│                                                             │
│ ✅ 初始化完成                                               │
│    - Agent知道有这些工具存在                                │
│    - 但Agent不知道什么时候用它们                            │
│    - SKILL.md的完整内容还没加载                             │
└─────────────────────────────────────────────────────────────┘
```

### 2. 查询处理阶段（每次查询）

```
┌─────────────────────────────────────────────────────────────┐
│ 用户查询: "Azure Functions的官方文档"                        │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤1: Skill匹配（使用metadata）                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ 对比查询与每个Skill的description：                          │
│                                                             │
│ microsoft-docs:                                            │
│   description: "Query official Microsoft documentation..." │
│   keywords: ["azure", "文档", "官方", "microsoft"]         │
│   ✅ 匹配分数: 85 (≥ 50)                                    │
│                                                             │
│ microsoft-code-reference:                                  │
│   description: "Look up API references, code samples..."   │
│   keywords: ["code", "api", "示例", "代码"]               │
│   ❌ 匹配分数: 15 (< 50)                                    │
│                                                             │
│ 结果: 匹配到 microsoft-docs                                 │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤2: 加载完整SKILL.md                                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ 读取: skills/microsoft-docs/SKILL.md                       │
│                                                             │
│ 内容包括:                                                   │
│   ## Tools                                                 │
│   | microsoft_docs_search | Find documentation...         │
│   | microsoft_docs_fetch  | Get full page content...      │
│                                                             │
│   ## When to Use                                           │
│   - Understanding concepts                                 │
│   - Learning a service                                     │
│   - Finding configuration guides                           │
│                                                             │
│   ## Example Queries                                       │
│   - "How does Cosmos DB partitioning work?"               │
│   - "Azure Functions overview"                            │
│                                                             │
│ ✅ 完整SKILL.md已加载（只有匹配时才加载）                    │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤3: 注入SKILL.md作为指令                                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ context_message = f"""                                     │
│ <active_skills>                                            │
│ {SKILL.md的完整内容}                                        │
│ </active_skills>                                           │
│                                                             │
│ User Question: Azure Functions的官方文档                    │
│ """                                                        │
│                                                             │
│ 💡 SKILL.md告诉Agent:                                       │
│    "当用户问概念、教程、配置时，使用microsoft_docs_search"   │
│    "如果搜索结果不够，使用microsoft_docs_fetch获取完整内容" │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤4: Agent执行                                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ stream = agent.run_stream(context_message, thread=thread) │
│                                                             │
│ Agent的思考过程:                                            │
│ 1. 读取user question: "Azure Functions的官方文档"          │
│ 2. 查看active_skills中的指导                               │
│ 3. 发现：这是关于概念/文档的查询                            │
│ 4. SKILL.md说：使用microsoft_docs_search                  │
│ 5. 调用工具：microsoft_docs_search("Azure Functions")     │
│ 6. 获得结果，返回给用户                                     │
│                                                             │
│ ✅ Agent根据SKILL.md的指导选择了正确的工具                  │
└─────────────────────────────────────────────────────────────┘
```

### 3. 未匹配Skill的情况

```
┌─────────────────────────────────────────────────────────────┐
│ 用户查询: "hello world"                                     │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤1: Skill匹配                                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ microsoft-docs: 分数 10 (< 50) ❌                           │
│ microsoft-code-reference: 分数 5 (< 50) ❌                  │
│                                                             │
│ 结果: 无匹配的Skill                                         │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤2: 通用对话模式                                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ context_message = f"""                                     │
│ <active_skills>                                            │
│ (空 - 没有激活的skill)                                      │
│ </active_skills>                                           │
│                                                             │
│ User Question: hello world                                 │
│ """                                                        │
│                                                             │
│ 💡 没有SKILL.md指导，Agent不会使用MCP工具                   │
│    Agent进行普通对话                                        │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ Agent回复: "Hello! How can I help you today?"              │
│ (没有调用任何MCP工具)                                        │
└─────────────────────────────────────────────────────────────┘
```

## 关键代码位置

### azure_doc_agent.py - initialize()

```python
async def initialize(self) -> bool:
    # ... MCP初始化 ...
    
    # ✅ 关键：所有MCP工具在这里注册
    all_mcp_tools = self.mcp_client.get_tools()
    
    self.agent = chat_client.as_agent(
        instructions=system_instructions,
        tools=all_mcp_tools  # 所有工具都注册，不是条件性的
    )
    
    logger.info(f"Agent created with {len(all_mcp_tools)} MCP tools registered")
    return True
```

### azure_doc_agent.py - chat_stream_with_thinking()

```python
async def chat_stream_with_thinking(...):
    # 步骤1: 匹配skills
    matched_skills = self.registry.search_skills(user_message, min_score=50)
    
    # 步骤2: 如果匹配，加载完整SKILL.md
    if matched_skills:
        for skill in matched_skills:
            self.injector.activate_skill(skill.name)
        
        active_skills_context = self.injector.get_skills_for_llm()
        context_message = f"{active_skills_context}\n\nUser Question: {user_message}"
    else:
        context_message = user_message
    
    # 步骤3: Agent执行（工具已经注册，SKILL.md指导使用）
    stream = self.agent.run_stream(context_message, thread=thread)
    # ✅ 不再需要 tools=xxx 参数，工具已在初始化时注册
```

## 未来扩展示例

### 添加新的MCP Server: GitHub

1. **配置.env**
```bash
MCP_SERVERS=Microsoft Learn MCP,GitHub MCP

# Microsoft Learn MCP
MICROSOFT_LEARN_MCP_URL=http://localhost:3000
MICROSOFT_LEARN_MCP_ENABLED=true

# GitHub MCP (新增)
GITHUB_MCP_URL=http://localhost:3001
GITHUB_MCP_ENABLED=true
```

2. **创建新的Skill**
```markdown
---
name: github-search
description: Search GitHub repositories, find code examples, and explore open source projects
---

# GitHub Search

## Tools

| Tool | Use For |
|------|---------|
| `github_search_repos` | Search for repositories |
| `github_get_code` | Get file content from repository |

## When to Use

- Finding open source projects
- Looking for code examples on GitHub
- Exploring repositories
```

3. **系统自动处理**
```
初始化时:
  ✅ 连接到 GitHub MCP Server (http://localhost:3001)
  ✅ 获取工具: github_search_repos, github_get_code
  ✅ 注册到Agent: all_mcp_tools += [github_search_repos, github_get_code]

查询时:
  用户: "找Python的机器学习项目"
  ✅ 匹配到: github-search skill
  ✅ 加载SKILL.md指导
  ✅ Agent使用: github_search_repos("Python machine learning")
```


**渐进式披露的三层含义：**

1. **Skill层面**：metadata先加载，完整内容匹配后加载
2. **工具层面**：所有工具注册，SKILL.md指导何时使用
3. **扩展层面**：多MCP Server共存，不同Skill指导不同工具

这种设计实现了**"使用哪个MCP是不同的Skill给说明的"**这一核心理念！ 🎯
